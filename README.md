# Kelsey's Cleaning Vancouver

This repository is a static site published with **GitHub Pages via GitHub Actions**.

## Deployment

- Deployment workflow: `.github/workflows/deploy-pages.yml`
- Trigger: push to `master` (and manual `workflow_dispatch`)
- Published content: repository root (`index.html`)

To use this workflow, set **Settings → Pages → Source** to **GitHub Actions**.
